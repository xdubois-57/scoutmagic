{% extends "base.html.twig" %}

{% block title %}Édition du site — {{ site_name }}{% endblock %}

{% block content %}
    {% include 'partials/page_header.html.twig' with { title: 'Édition du site' } only %}

    {# Config mode section — the only thing left on this page since the
       module registry and badges moved to their own pages (Configuration >
       Modules / Configuration > Badges). #}
    <div class="card mb-4">
        <div class="card-body d-flex align-items-center justify-content-between">
            <div>
                <h2 class="h5 mb-1">Mode configuration</h2>
                <p class="text-body-secondary small mb-0">
                    Permet d'éditer les textes et les images du site pour cette session.
                </p>
            </div>
            <form method="post" action="/config-mode/{{ config_mode ? 'deactivate' : 'activate' }}">
                {{ csrf_field()|raw }}
                <button type="submit" class="btn btn-sm {{ config_mode ? 'btn-warning' : 'btn-outline-primary' }}">
                    {{ config_mode ? 'Désactiver' : 'Activer' }}
                </button>
            </form>
        </div>
    </div>
{% endblock %}
