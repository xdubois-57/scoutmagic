{% extends 'base.html.twig' %}
{% block title %}{{ email.subject }} — {{ site_name }}{% endblock %}
{% block content %}
    {# The way back to the member's page lives in the breadcrumb trail
       (design.md §7.3) — no « Retour » button here. #}
    {% include 'partials/page_header.html.twig' with {
        title: email.subject,
        subtitle: 'Envoyé le ' ~ email.sent_at|french_date ~ ' · ' ~ email.section_name,
    } only %}
    <div class="card">
        <div class="card-body">
            {{ email.body_html|raw }}
        </div>
    </div>
{% endblock %}
