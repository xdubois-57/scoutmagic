{% extends 'email/base.html.twig' %}
{% block content %}
    <h2 style="margin:0 0 16px;font-size:20px;">Désinscription d'une adresse email</h2>
    <p>L'adresse <strong>{{ unsubscribed_email }}</strong> vient de se désinscrire des emails groupés de {{ site_name }}.</p>
    <p>Cela signifie qu'elle ne recevra plus aucun email groupé, et qu'elle ne peut plus non plus être utilisée pour se connecter au site par lien magique. Cela peut aussi signifier que la personne concernée souhaite quitter l'unité — n'hésitez pas à vérifier si une action complémentaire est nécessaire.</p>
    {% if member_names|length > 0 %}
        <p>Membre(s) concerné(s) :</p>
        <ul>
            {% for name in member_names %}
                <li>{{ name }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
