# ScoutMagic

Site web open source pour les unités scoutes belges de la fédération « Les Scouts ».

## Auteur

ScoutMagic est développé et maintenu par Xavier Dubois.

Voir [NOTICE](NOTICE) pour la liste des contributeurs et
[LICENSE](LICENSE) pour les conditions de licence, y compris les conditions
additionnelles relatives à l'usage du nom « ScoutMagic ».

## Avertissement

ScoutMagic est un logiciel libre fourni sans garantie d'aucune sorte,
conformément à la licence AGPL-3.0 (voir LICENSE, sections 15-16).

Toute unité qui déploie ScoutMagic agit en tant que responsable de
traitement au sens du RGPD pour les données qu'elle y héberge : c'est à
elle qu'incombent l'évaluation de la conformité RGPD, la sécurisation de
son hébergement, la tenue du registre de traitement et la notification en
cas de violation de données. L'auteur et les contributeurs du projet ne
sont ni responsables de traitement ni sous-traitants pour les instances
déployées par des tiers, et n'ont aucun accès aux données qui y sont
hébergées.

Une faille de sécurité découverte dans le code doit être signalée via
[SECURITY.md](SECURITY.md). Les corrections sont apportées sur une base
volontaire, sans garantie de délai.

## Fonctionnalités

- Gestion des membres depuis l'import CSV Desk
- Contrôle d'accès basé sur les rôles (6 niveaux)
- Authentification sans mot de passe (lien magique, mot de passe, passkey)
- Design responsive mobile-first, installable comme application sur l'écran d'accueil (PWA) avec accès hors ligne aux pages publiques, au calendrier, au centre de notifications, au trombinoscope, aux staffs, aux statistiques des membres, aux prévisions, et à la page et au compte propres de chaque membre — chaque image pré-téléchargée, extensible par des modules, sans qu'aucune page de configuration ni contenu privé/financier ne soit jamais mis en cache
- Aide contextuelle intégrée : un bouton d'aide sur chaque page ouvre les sujets qui la concernent (panneau consultable hors ligne), et la page `/aide` rassemble toute la documentation, filtrée selon le rôle du visiteur — livrée avec chaque release, extensible par les modules
- Mode configuration pour l'édition de contenu en ligne
- Architecture modulaire pour l'extensibilité
- Données personnelles chiffrées au repos
- Emails transactionnels signés DKIM
- Gestion du consentement aux cookies (bannière et préférences alignées sur les exigences ePrivacy)
- Migration de schéma automatisée
- Planificateur de tâches (cron + pseudo-cron)
- Journal d'audit
- Centre de notifications avec Web Push, préférences par type et par canal (sur le site, push, e-mail), plages horaires de silence, et un mode discret
- Sauvegardes chiffrées à la demande et automatiques, mise à jour en un clic depuis les releases GitHub, réinitialisation/restauration
- Page Support : envoi optionnel (activable/désactivable) de statistiques d'utilisation agrégées vers ScoutMagic, avec aperçu exact de ce qui est transmis et sans aucune donnée de membre ; génération à la demande d'une archive de diagnostic (chiffrée, réservée au superadmin, jamais transmise automatiquement)
- Modules optionnels : gestion financière (import de relevés bancaires, reçus, créances), articles actualités/événements avec formulaires d'inscription et paiement, calendrier d'activités avec flux ICS, galerie photo/vidéo (stockage local ou S3), groupes de discussion privés par section, location des biens de l'unité (locaux, terrains, tentes, remorques, matériel), courrier entrant en lecture seule (IMAP), tableaux de rétrospective post-activité, trombinoscope du staff, envoi de mails groupés, bannières de la page d'accueil, statistiques des membres, transfert d'appel du numéro d'urgence de l'unité, encadrement (parcours de formation, échéances légales et intendants), inscriptions en ligne (demande publique, traitement par le Staff d'U, départs, passage et prévisions d'effectifs), camps (les lieux où l'unité campe et les séjours qui s'y déroulent), cotisations (vérification de ce que la fédération facture)
- Inscriptions en ligne : un formulaire public sans compte, une demande que le Staff d'U accepte ou refuse, et un lien de suivi pour la famille. La demande et la fiche du membre restent **découplées** — accepter n'inscrit personne dans le Desk, et la famille n'apprend la décision qu'à l'envoi de l'e-mail, jamais avant. Le module porte aussi les grilles Départs et Passage (enregistrement au fil de la saisie, une requête par case) et les Prévisions d'effectifs, qui peuvent **opposer un veto** à la transition d'année scoute tant qu'un départ ou un passage reste à trancher
- Camps : les terrains où l'unité campe et les séjours qui s'y déroulent — « où est-on déjà allés, et est-ce que c'était bien ? ». Rien n'est jamais supprimé (un séjour qui n'a pas eu lieu est *annulé*, ce qui est précisément ce qu'un futur staff doit savoir), un séjour porte de vraies dates **ou** une simple année, un avis par séjour écrit par les animateurs, la fusion des doublons de lieux et de séjours, une carte chargée à la demande, et les contacts d'un terrain — les seules personnes externes que le site conserve — anonymisables sur simple demande
- Encadrement : trois listes de personnes à contacter, recalculées à chaque affichage depuis le dernier import Desk — animateurs à qui proposer la formation, anniversaires des 20 ans qui approchent, intendants inscrits et depuis combien de temps. Le module ne stocke aucune donnée de membre et n'affiche jamais l'état d'un CQA ni d'un extrait de casier judiciaire, que le site ne conserve nulle part
- Location des biens : calendrier de disponibilité public, demandes en ligne sans compte, moteur tarifaire (nuits ou jours pleins, minimum facturable, catégories de locataire), contrat et facture versionnés en PDF, acompte/solde/caution via le module Finances, relevés de compteurs et état des lieux, décompte final, publication de l'occupation au calendrier de l'unité sans jamais nommer le locataire, registre de conformité par bien et rappels automatiques
- Courrier entrant : passerelle IMAP **en lecture seule** vers une ou plusieurs boîtes de l'unité — aucun message n'est jamais marqué comme lu, déplacé ni supprimé sur le serveur distant. Les réponses des locataires sont rattachées automatiquement à la bonne réservation ; un message qu'aucun module ne reconnaît n'est jamais enregistré
- Intégration IA optionnelle (extraction de données de reçus, génération de texte RGPD, modération des rétrospectives, résumés d'articles, lecture d'un index de compteur sur photo, classement d'une pièce jointe reçue par email) — l'IA propose toujours, elle ne décide jamais : aucune suggestion ne modifie une donnée métier sans validation humaine

## Prérequis

- PHP >= 8.4
- MySQL >= 8.0
- Composer (pour le développement/build uniquement — non nécessaire sur le serveur)
- Node.js >= 22 et npm (pour l'outillage de développement uniquement — analyse statique JavaScript, tests unitaires JavaScript et tests de bout en bout ; non nécessaire sur le serveur, ni pour exécuter ScoutMagic)
- Accès FTP au serveur d'hébergement

## Installation

1. Cloner le dépôt.
2. Exécuter `composer install`.
3. Pointer la racine de votre serveur web vers `public/`.
4. Accéder au site — l'assistant de configuration vous guidera.

## Développement

```bash
composer install
composer serve                     # serveur de dev local (localhost:8000)
vendor/bin/phpunit                 # exécuter les tests PHP (suite complète)
vendor/bin/phpstan analyse core/   # analyse statique

npm ci                             # dépendances Node (outillage de dev uniquement — voir Prérequis)
npm run typecheck                  # analyse statique JavaScript (TypeScript checkJs — voir ci-dessous)
npm test                           # exécuter les tests unitaires JavaScript (Vitest + jsdom)
npm run test:coverage              # idem, avec couverture LCOV (coverage/js/lcov.info)

npm run e2e:install                # une seule fois : installer le navigateur Chromium (Playwright)
npm run e2e                        # exécuter les tests de bout en bout (voir ci-dessous)
```

`vendor/bin/phpunit` exécute **toute** la suite, groupe `database` compris : aucun test n'est exclu par défaut, ni en local, ni en CI, ni dans `scripts/release.sh`. La majorité de ce groupe tourne sur une base SQLite en mémoire (`Tests\DatabaseTestHelper`) et ne demande rien de particulier ; seuls les six fichiers qui lisent `TEST_DB_*` ont besoin d'une vraie instance MySQL jetable. Renseignez alors `TEST_DB_HOST` / `TEST_DB_PORT` / `TEST_DB_NAME` / `TEST_DB_USER` / `TEST_DB_PASSWORD` (la CI utilise `127.0.0.1`, `3306`, `test_db`, `root`, `test_password`). **`TEST_DB_PASSWORD` ne doit pas être vide** : les tests du formulaire d'installation (`Tests\Core\Http\Controller\SetupControllerTest`) rejouent la vraie validation du formulaire, qui rend le mot de passe de base de données obligatoire.

`composer serve` encapsule `php -S` avec des valeurs `upload_max_filesize`/`post_max_size` relevées (`public/.user.ini`, utilisé en production, n'est pas pris en compte par le serveur intégré) — si vous lancez `php -S` directement à la place, les uploads de plus de 8 Mo échoueront avec une erreur 413. Augmentez les valeurs dans `scripts.serve` de `composer.json` si vous devez tester des uploads plus volumineux en local (ex. vidéos de galerie).

Les tests JavaScript (`tests/js/`, Vitest + jsdom) sont isolés du reste de la pile : aucun serveur PHP, aucune base MySQL, aucun réseau réel (`fetch`/Service Worker/WebAuthn sont simulés dans les tests qui en ont besoin). Ils exercent directement le vrai code de production sous `public/assets/js/`, jamais une réimplémentation. Node/npm ne servent qu'à ça — ScoutMagic lui-même reste du JavaScript navigateur simple, sans étape de build, et ni Node ni npm ne sont nécessaires pour l'exécuter ou le déployer (voir `AGENTS.md` § CSS / frontend).

### Analyse statique JavaScript

`npm run typecheck` est l'équivalent JavaScript de `vendor/bin/phpstan analyse` pour PHP : un contrôle statique, déterministe, qui détecte avant toute exécution une classe d'erreurs que ni Vitest (comportement à l'exécution) ni SonarQube Cloud (qualité de code générale, doublons, complexité, sécurité) ne couvrent — identifiants non résolus, appels de fonction avec un nombre d'arguments incorrect, signatures qui divergent d'un site d'appel après un refactoring, accès à une propriété statiquement invalide. C'est exactement la classe de bug documentée dans `AGENTS.md` § Static analysis (un paramètre de constructeur PHP supprimé, un site d'appel non mis à jour, jamais détecté avant la production) — appliquée ici au JavaScript.

Concrètement, c'est le compilateur TypeScript utilisé uniquement comme vérificateur de développement (`allowJs`/`checkJs`/`noEmit` — voir `tsconfig.json`) sur le JavaScript existant de `public/assets/js/`, qui reste par ailleurs strictement inchangé : pas de transpilation, pas de build, aucun fichier généré, rien de nouveau à servir au navigateur ou à déployer (voir `AGENTS.md` § CSS / frontend). Le typage vient d'annotations [JSDoc](https://jsdoc.app/) (`@param`, `@returns`) ajoutées aux fonctions à plusieurs paramètres réutilisées à plusieurs endroits — là où une erreur de signature serait sinon invisible pour l'outil ; les scripts strictement DOM (un écouteur d'événement à un seul paramètre évident) n'en ont pas besoin.

`scripts/js-typecheck.mjs` (invoqué par `npm run typecheck`) encapsule `tsc` avec un mécanisme de baseline identique dans son principe à `phpstan-baseline.neon` : `js-typecheck-baseline.json` recense les signalements préexistants acceptés, et une exécution propre signifie *aucun nouveau signalement*, pas zéro signalement. Au moment de l'introduction de ce contrôle, l'essentiel des signalements initiaux (le typage générique de `document.getElementById()`/`querySelector()` ne correspondant pas au type d'élément réellement utilisé) a été corrigé par un cast JSDoc plutôt qu'accepté comme dette — la baseline est donc actuellement vide (`{}`) — mais ce mécanisme reste en place pour absorber une dette légitime future sans bloquer chaque changement. Ne régénérez la baseline (`node scripts/js-typecheck.mjs --generate-baseline`) que pour accepter délibérément une dette préexistante que vous ne corrigez pas maintenant — jamais pour masquer un signalement introduit par votre propre changement.

Exécutez `npm run typecheck` avant tout commit qui touche `public/assets/js/` — comme `vendor/bin/phpstan analyse` pour le PHP (`AGENTS.md` § Static analysis). CI (`javascript-tests`, avant les tests unitaires) et `scripts/release.sh` (verrou « Tests ») exécutent la même commande canonique et bloquent en cas d'échec.

### Tests de bout en bout (E2E)

Un petit nombre de scénarios à haute valeur, jamais une couverture exhaustive : **ScoutMagic démarre-t-il vraiment et affiche-t-il une page dans un vrai navigateur ?** PHPUnit instancie les contrôleurs un par un et n'exécute jamais `public/index.php` ; Vitest exécute du JavaScript dans un DOM simulé, sans PHP ni base de données. Une erreur de câblage dans la racine de composition de `public/index.php` (un constructeur dont la signature change, un service manquant) ne se voit donc qu'en production — c'est déjà arrivé (voir `AGENTS.md` § Static analysis). Les tests E2E ferment ce trou.

```bash
npm ci                 # une fois : dépendances Node
npm run e2e:install    # une fois : télécharge le navigateur Chromium utilisé par Playwright
npm run e2e            # exécuter le scénario complet
```

`npm run e2e` (c'est-à-dire `scripts/e2e.sh`) fait tout, en une commande, sans aucune étape manuelle :

1. crée une installation ScoutMagic jetable dans un répertoire temporaire (son propre `storage/`, sa propre `config/app.php`, ses propres secrets générés) — **votre installation locale n'est jamais lue ni modifiée** ;
2. crée et vide une base de données dédiée (`scoutmagic_e2e` par défaut, jamais `test_db` ni une base de développement), puis y applique `schema/core.sql` via le vrai `Core\Database\MigrationRunner` ;
3. pointe l'instance sur elle-même comme destination des statistiques — c'est ce qui en fait le *récepteur* (`Core\Statistics\DestinationMatcher`, ARCHITECTURE.md §8.49) et donc ce qui rend visible le module réservé au récepteur (`support_dashboard`), que `ModuleManager` masque partout ailleurs : un module invisible est un module dont le câblage n'est jamais démarré. Cela ne coûte rien d'autre — `StatisticsSender` refuse d'envoyer vers lui-même, et `localhost` n'est de toute façon pas un hôte public (`isPublicHost()` le nomme explicitement), donc aucune exécution n'émet jamais de rapport ;
4. active **tous** les modules du dépôt, via le vrai `Core\Module\ModuleManager::activate()` — le même appel que la page Modules de l'administration, donc le `schema.sql`, les paramètres par défaut, l'entrée de registre et le journal de chaque module. Pourquoi tous et pas seulement les trois `enabled_by_default` : `public/index.php` câble **chaque** module actif dans le contrôleur frontal avant de router quoi que ce soit, donc un module laissé inactif est un pan de cette racine de composition qu'aucun test navigateur n'exécute — et une faute à cet endroit ne casse pas une page, elle renvoie 500 sur *toutes* les routes du site. C'est déjà arrivé (voir `all-modules-enabled.spec.js` ci-dessous). Un module qui ne peut pas être activé arrête le provisionnement avec son propre message, plutôt que de laisser l'exécution prétendre à une couverture qu'elle n'a pas ;
5. démarre `php -S` sur un port libre, avec pour racine le `public/` de cette installation jetable — le vrai `public/index.php`, pas une application de test. L'instance s'appelle elle-même `http://localhost:<port>` et non `http://127.0.0.1:<port>` : un *Relying Party ID* WebAuthn est un nom de domaine, et Chrome refuse catégoriquement une adresse IP littérale (« This is an invalid domain »), donc sur une instance qui se nomme par son adresse **aucune clé numérique ne peut être enregistrée ni utilisée, dans aucun navigateur**. Même bouclage, même contexte sécurisé, même « hôte non public » — voir `e2e_base_url()` dans `scripts/e2e-support.php` pour le détail de ce que cela ne change pas. Le serveur reçoit aussi un `sendmail_path` pointant sur `scripts/e2e-maildrop.php` (voir « Le courrier de l'exécution » plus bas) ;
6. attend (par sondage, jamais par `sleep`) que le serveur réponde ;
7. lance Playwright/Chromium en mode *headless* ;
8. rend le code de sortie de Playwright tel quel ;
9. arrête le serveur, supprime la base et le répertoire temporaire — après un succès, après un échec, et sur Ctrl-C.

Scénarios actuels (`tests/e2e/specs/`), chacun bootant l'application réelle de bout en bout :

- **`all-modules-enabled.spec.js`** — tous les modules livrés par le dépôt sont réellement actifs sur l'instance testée (aucun interrupteur « Inactif » sur `/config/modules`, aucun module actif mais non chargé faute d'un module requis), et la page d'accueil publique s'affiche avec l'ensemble câblé. C'est le garde-fou qui donne son sens au reste de la suite : le scénario lit la liste des modules sur la page elle-même — jamais une liste en dur — donc un module ajouté demain est couvert le jour où il arrive, sans que personne ait à y penser. Il existe parce qu'un `use` manquant sur un type de constructeur (résolu dans le mauvais namespace) a mis le site en 500 sur *toutes* ses routes pendant que cette suite restait verte, le module concerné n'étant pas activé par le harnais.
- **`public-home-page.spec.js`** — `GET /` (page d'accueil publique, `role_min: public`) : réponse HTTP 200, titre de page issu du `site_name` lu en base, titre `<h1>` du contenu éditable, repères d'accessibilité `main`/`navigation`/`contentinfo`, lien « Protection des données » du pied de page.
- **`login-page.spec.js`** — `GET /login` (`Core\Http\Controller\AuthController::login()`) : formulaire de connexion (onglet lien magique par défaut) avec ses champs étiquetés et son lien vers `/rgpd`, exerçant une racine de composition différente (CsrfGuard, LastLoginMethodCookie, HumanCheck) de celle de la page d'accueil.
- **`rbac-anonymous-redirect.spec.js`** — un visiteur anonyme demandant `/account` (`role_min: identified`) est réellement redirigé, dans un vrai navigateur, vers une page `/login` qui s'affiche correctement — pas seulement le couple statut/en-tête `302`/`Location` qu'un test unitaire de `RbacGuard` vérifie déjà isolément.
- **`cookie-consent-reject.spec.js`** — la bannière de consentement s'affiche sans y être invitée sur une instance jamais visitée ; cliquer sur « Tout refuser » la fait disparaître et ne laisse dans le navigateur que le cookie nécessaire `cookie_consent`, jamais le cookie fonctionnel `last_login_method` (AGENTS.md § Cookie consent).
- **`scout-year-transition.spec.js`** — la dégradation gracieuse dans les deux sens (le module Inscriptions est désactivé puis réactivé depuis la vraie page Modules, et les trois étapes qu'il possède — donc la phase « Préparation » entière — disparaissent puis reviennent, la numérotation se recalculant sur ce que le site a réellement), puis le passage complet à l'année scoute suivante, connecté en super-administrateur, en suivant les quatre étapes de la page « Année scoute » (`/admin/scout-year`) : prévisualisation limitée à la session, import Desk des nouvelles inscriptions, bascule des chefs et intendants sur l'année cible, puis bascule publique. Le test vérifie aussi ce que chaque étape ne doit *pas* faire (chaque bouton reste désactivé tant que l'étape précédente n'est pas faite, le public ne voit jamais l'année du staff) et lit les libellés sur la page plutôt que de les recopier, pour rester valable d'une année à l'autre. **La page `/admin/scout-year` est la spécification de ce test** : toute modification du déroulé doit être répercutée dans le test, dans le même changement (rappel présent dans `core/View/templates/admin/scout_year.html.twig` et dans `ScoutYearController::buildTransitionSteps()`).
- **`groups-discussion.spec.js`** (quatre scénarios) — un membre écrit dans un groupe de discussion : création du groupe depuis le vrai formulaire de `/groups`, puis, dans le composeur réel et sans un seul rechargement, un message simple, un message contenant un lien, un sondage suivi d'un vote, une réponse et une réaction — puis un rechargement de la page qui exige du serveur qu'il rende tout cela. C'est la seule couche où le composeur est observable : `public/assets/js/groups.js` intercepte l'envoi du formulaire et construit lui-même le corps de la requête, ce que ni PHPUnit (qui voit un `$_POST` écrit par le test) ni Vitest (qui voit un formulaire jsdom sans serveur derrière) ne peuvent contrôler. Trois bugs y vivaient : le message n'était pas envoyé du tout parce que le champ était désactivé avant la construction du `FormData` (« Un message ne peut pas être vide. » sur un message manifestement non vide), le bouton « Publier » ne réagissait pas au premier clic sur un message contenant un lien (l'aperçu du lien apparaissait entre le `mousedown` et le `mouseup` et déplaçait le bouton), et le dialogue « qui a réagi ? » restait bloqué sur son spinner après une réaction. Le scénario ne vérifie délibérément pas les métadonnées Open Graph d'un lien : les récupérer est une requête sortante que `Modules\Gallery\Service\OgScraperService` refuse vers une adresse privée ou un port non standard (SECURITY.md §17), ce qu'est exactement l'instance jetable — c'est la dégradation documentée (lien nu, URL retirée du texte) qui est vérifiée, la voie avec métadonnées restant couverte par `Tests\Modules\Groups\Service\PostLinkServiceTest`. Le scénario vérifie aussi la **signature** de chaque message et de chaque commentaire : le nom du compte suivi, entre parenthèses, de ses adhésions (`Baden Powell (Baden)`), sur une carte rendue par le serveur comme sur un fragment renvoyé par le point d'entrée AJAX, et jusque dans le dialogue « qui a réagi ? » — les trois surfaces passent par `Service\MemberIdentityService`, et c'est ici qu'on voit qu'elles ne divergent pas (ARCHITECTURE.md §8.40).

  Le quatrième scénario attache une photo à un commentaire : elle s'affiche dans le composeur **avant** l'envoi (dessinée depuis le fichier lui-même, rien n'est encore téléversé), le bouton « Retirer » la détache réellement, et le commentaire part **sans une seule ligne de texte**. Il utilise volontairement un groupe tout neuf : la première photo d'un groupe crée son album au milieu de la requête (`Service\PostMediaService::ensureAlbumId()`), et l'instance immuable `DiscussionGroup` que le contrôleur tenait encore la lisait à `null` — la carte rendue pour le navigateur ne trouvait donc aucun média, et un commentaire ne contenant qu'une photo arrivait en bulle vide. Sur un groupe ayant déjà reçu une photo, le bug ne se reproduit pas.

  Le troisième scénario tient sur un écran de téléphone (390 px) et couvre deux choses que rien d'autre ne voit : le repli du sélecteur de réactions derrière un seul bouton « Réagir », qui se **déplie vers la droite** en une transition de `max-width` plutôt qu'en apparaissant d'un coup (une `display` ne se transitionne pas ; la `visibility` est retardée de la durée de la transition à la fermeture, et le tout est annulé sous `prefers-reduced-motion`) — du CSS derrière une media query, donc invisible pour jsdom comme pour un test en largeur bureau — et, en bloquant le chargement de `groups.js`, la promesse permanente du module : **tout continue de fonctionner sans JavaScript**. Les deux moitiés vont ensemble, le repli n'étant acceptable que parce que les six boutons reviennent quand le script n'arrive pas.

  Le second scénario du même fichier couvre ce qu'une seule personne ne peut pas atteindre : un commentaire n'est jamais *nouveau* pour celui qui l'a écrit, et « Signaler » n'est jamais proposé sur son propre message. Un second membre ordinaire commente le message du premier ; celui-ci retrouve la conversation dépliée d'elle-même avec « 1 nouveau », signale le commentaire, et la confirmation s'affiche à côté sans rechargement — puis, après un vrai rechargement, le badge a disparu (ouvrir un groupe le marque lu) et le signalement, lui, a bien été enregistré. Le commentaire y est signé par un **second compte** (`Kaa Serpent (Kaa)`), ce qui vérifie la règle de nommage sur quelqu'un d'autre que le lecteur.

- **`auth-methods.spec.js`** (trois scénarios) — les trois façons de se connecter, chacune menée jusqu'au bout. **Lien magique** : la demande depuis le vrai formulaire, l'email réellement produit par `Core\Mail\MailService` (lu dans la boîte de l'exécution, voir plus bas), le lien ouvert dans un **second contexte de navigation** — donc un second appareil, avec son propre pot à cookies — et l'appareil qui avait fait la demande qui se connecte tout seul par son *polling*, sans avoir rien vu du clic. Le même scénario demande d'abord un lien pour une adresse qui n'a aucun compte et exige que la page réponde exactement la même chose, sans qu'aucun email ne parte : la page de connexion ne doit jamais permettre de savoir qui est membre. **Clé numérique** : un authentificateur virtuel de Chrome (domaine CDP `WebAuthn`) enregistre une vraie *passkey* depuis `/mon compte`, puis, après déconnexion, ouvre une session **sans adresse ni mot de passe** — et le cookie fonctionnel qui retient la méthode n'est écrit que parce que le consentement a été donné (le contraire est vérifié par `cookie-consent-reject.spec.js`). **Mot de passe** : le refus sans consentement RGPD, le refus sur un mauvais mot de passe (aucune session créée), la réussite, puis une déconnexion qui rend vraiment `/account` inaccessible. Rien n'est contourné : le CSRF, les barrières anti-robot (attendues, jamais désactivées — voir `tests/e2e/support/human-check.js`), le `LoginThrottler` et le `RoleResolver` sont ceux de production.
- **`news-form-payment.spec.js`** — une actualité avec formulaire payant, du chef qui la construit à la famille qui s'inscrit. Le compte bancaire est créé dans Finances, l'article et son formulaire sont bâtis dans le **vrai constructeur de champs** (`public/assets/js/news-form-builder.js` garde toute la liste des champs en JavaScript et la sérialise dans un unique champ caché `fields_json` à l'envoi — exactement la classe de corps de requête fabriqué à la main qui avait mis trois bugs dans le composeur des groupes), puis une visiteuse anonyme remplit le formulaire : le total se met à jour à la saisie, la confirmation affiche le montant, l'IBAN, la communication structurée et le QR code SEPA, la capacité décroît réellement (« Il reste 1 place », puis champ désactivé), et la page des réponses montre le dû et l'impayé. Le scénario vérifie enfin ce que l'email de confirmation d'une soumission **anonyme** ne doit pas contenir : le texte libre saisi par le répondant (audit M14 — l'adresse et le texte y sont tous deux fournis par l'inconnu, et les renvoyer l'un à l'autre ferait de cet email signé DKIM un relais). Il a trouvé un vrai défaut à sa première exécution : l'attribut `max` du champ à capacité était construit comme une chaîne interpolée, donc échappé par Twig et relu par le navigateur comme la *valeur* `"1"` — aucune limite n'était appliquée côté client (corrigé, et épinglé par `Tests\Modules\News\Controller\NewsIntegrationTest`).
- **`groups-management.spec.js`** — l'autre moitié du module Groupes : décider **qui** peut écrire. Un chef ouvre un groupe sur invitation, lui donne une description, invite quelqu'un via la recherche que `groups.js` substitue au menu déroulant, épingle son message, le nomme modérateur, clôture le groupe puis le rouvre, et le retire enfin. Chaque décision est vérifiée dans **le navigateur de l'autre personne** — c'est la seule couche où l'on peut le faire : le groupe n'apparaît pas dans sa liste avant l'invitation et n'y est plus après le retrait, la route lui répond **404 et jamais 403** (un 403 confirmerait l'existence du groupe à qui ne doit pas la connaître), le composeur disparaît à la clôture avec la phrase qui l'explique, et revient à la réouverture. Son message, lui, reste dans le groupe.
- **`optional-module-dependencies.spec.js`** — les deux façons dont un module s'appuie sur un autre (ARCHITECTURE.md §7.5), dans les deux sens. **Dure** (`requires`) : Galerie ne peut pas être désactivée tant que Groupes en a besoin — le refus nomme le module et dit quoi faire, l'interrupteur revient en arrière ; Groupes désactivé, Galerie part ; Groupes devient alors non activable, avec la phrase qui le dit plutôt qu'un contrôle qui échouerait. **Optionnelle** (une interface *nullable* et rien d'autre) : Finances désactivé, l'éditeur d'actualités cesse simplement de proposer un prix et un compte destinataire — pas d'erreur, pas de sélecteur vide, et la limite de capacité, qui appartient à Actualités, reste. Le tout est remis en place et le scénario vérifie qu'il l'a fait : une dépendance qui se dégrade proprement doit aussi **revenir** proprement, et les scénarios qui suivent dans l'ordre alphabétique en dépendent.
- **`gallery-download.spec.js`** (deux scénarios) — le module Galerie de bout en bout, de l'album vide à la photo enregistrée sur le téléphone de quelqu'un. Un chef crée un album, y téléverse **deux photos portant le même nom de fichier** (`IMG_1234.png`, ce que deux téléphones différents produisent tous les jours), la file de tâches est réellement tournée pour que les rendus existent, puis chaque photo est enregistrée depuis la visionneuse et l'album entier est téléchargé en ZIP. Trois choses n'existent qu'ici : le téléversement est une boucle XHR construite par `public/assets/js/gallery.js` fichier par fichier ; les rendus n'existent qu'après une tâche de fond, donc « télécharger en haute qualité » n'a aucun sens tant que la file n'a pas tourné (c'est le premier scénario à la tourner, via `support/scheduler.js` et le vrai `public/cron.php` de l'instance) ; et **le téléchargement lui-même est un comportement du navigateur** — qu'un clic *enregistre* un fichier, et sous quel nom, se décide entre l'attribut `download` et l'en-tête `Content-Disposition`, que PHPUnit et Vitest ne peuvent vérifier chacun qu'à moitié. Le nom est justement l'enjeu : les deux photos repartent sous deux noms distincts, et le ZIP de l'album contient **exactement ces deux noms-là** — la vérification se fait entre les deux chemins, jamais contre une chaîne écrite dans le test, ce qui est ce qui les empêcherait de diverger (`Modules\Gallery\Service\MediaFileName`). Le second scénario reprend le même album avec le compte membre ordinaire : il le trouve dans la liste et repart avec la photo, sous le même nom — ce qu'un média s'appelle est une propriété du média, pas de qui l'enregistre.
- **`rental-request.spec.js`** — un administrateur met un local en location depuis `/admin/locations`, désigne un gestionnaire, et un visiteur inconnu demande à le louer : disponibilités, tarif proposé, demande enregistrée avec sa référence.
- **`rental-management.spec.js`** — l'autre bout du même module : un chef configure un bien, bloque une semaine au calendrier et répond à une demande.
- **`password-reset.spec.js`** — le « Mot de passe oublié » de bout en bout : la demande depuis le mini-formulaire de la page de connexion (une adresse inconnue reçoit exactement le même accusé, et jamais d'email), le lien reçu dont le jeton voyage dans le **fragment d'URL** (le serveur ne le voit pas ; la page le vérifie via `/password-reset/{id}/check` avant de révéler le formulaire, puis l'efface de la barre d'adresse), le nouveau mot de passe, l'ancien refusé, le lien déjà servi qui ne sert plus. Le scénario restaure le mot de passe provisionné par un second passage complet — la limite par IP en autorise cinq.
- **`account-settings.spec.js`** — « Mon compte » : le profil, la photo (l'overlay de l'avatar mène à `/upload`, où `public/assets/js/upload.js` **réécrit les octets** — un PNG de 3000 px ressort réencodé WebP avant même le POST, et c'est ce fichier-là que le serveur stocke et ressert), sa suppression via le `data-confirm` délégué de `base.html.twig`, et le changement de mot de passe observé depuis **deux navigateurs** : l'onglet qui change reste connecté, l'autre appareil est déconnecté à sa navigation suivante (`sessions_valid_from`). Tout est restauré (noms, mot de passe, photo) — le membre est un fixture partagé.
- **`registration-flow.spec.js`** — une famille inscrit un enfant : l'admin ouvre les inscriptions, le formulaire public (PII anonyme + barrière anti-robot + l'indice de branche calculé par le script inline sous la date de naissance), l'email de reçu et sa **vue de suivi minimale** par jeton (prénom, date, statut — et jamais l'adresse, les téléphones ou le nom du parent saisis une minute plus tôt), l'acceptation par l'admin **que la famille ne voit pas** tant que l'email d'acceptation n'est pas parti (`RequestStatusService::visibleStatus()`), le retrait (visible, lui, immédiatement), la fermeture du formulaire, puis le **code d'inscription en cours d'année** généré sur la page de config et qui rouvre le formulaire pour le seul visiteur qui l'a saisi. La demande finit retirée et le code désactivé : un état non final opposerait son veto à `scout-year-transition.spec.js` (`ScoutYearTransitionVetoProvider`).
- **`registration-grids.spec.js`** — les deux grilles à enregistrement automatique du même module, Départs et Passage : la case cochée et le commentaire saisi partent chacun comme **sa propre requête** (la règle de concurrence du module), le rechargement prouve que les deux ont atterri, et la demande retirée par `registration-flow` est remise en attente, acceptée (elle apparaît alors dans la grille Passage) puis retirée à nouveau. Ces deux gabarits sont les deux sites du post-mortem CSP du projet — un gestionnaire inline réintroduit y meurt en silence, invisible de PHPUnit comme de Vitest.
- **`retro-board.spec.js`** — un tableau de rétrospective : le chef le crée, deux visiteurs anonymes (l'un accepte les cookies — son identité de vote est le cookie fonctionnel `retro_voter` —, l'autre refuse et vote quand même via sa session, la promesse « best-effort » du module) écrivent et votent, et chaque mot **traverse vers l'autre navigateur par le seul polling**. Le like survit au re-rendu du poll (`youVoted` — la régression que `_comment.html.twig` documente nommément), le chef masque un mot (il continue de le lire barré ; le visiteur ne voit plus que « Mot masqué. », sans texte ni compteur) puis le réaffiche, et la régénération du lien tue l'ancienne URL sur-le-champ (404) pendant que la nouvelle porte tous les mots et votes. C'est aussi le seul endroit où les **deux moteurs de rendu** d'une carte (Twig au chargement, `retro-board.js` à chaque poll) sont comparés sur un vrai serveur.
- **`maintenance-backup.spec.js`** — Config > Maintenance, la page qui peut sauver ou effacer l'installation : la fréquence de sauvegarde automatique s'enregistre au changement, la sauvegarde « base de données seule » (POST synchrone) fait apparaître sa ligne, la **sauvegarde complète chiffrée** part en tâche de fond et est menée à terme via la boucle de polling de `maintenance.js` (`/api/maintenance/backup-status/{id}`), le secret du webhook se révèle, et les trois verrous à mot-clé de la zone de danger (REINITIALISER / RESTAURER / EFFACER + case à cocher) arment et désarment sans qu'aucun ne soit jamais déclenché. « Vérifier maintenant » (mise à jour) n'est pas exercé : il appelle GitHub sur le vrai réseau.
- **`config-desk.spec.js`** — le Config Desk, c'est-à-dire la surface de configuration du RBAC lui-même : la fonction du membre ordinaire est promue `intendant` **et sa session déjà ouverte gagne l'accès à la navigation suivante** — puis rétrogradée, et la porte se referme aussi vite (`SessionRevalidator` re-résout le rôle à chaque requête ; deux navigateurs regardent les deux moitiés). Le nom de section s'enregistre au blur et paraît sur `/sections`, la visibilité retire la section de la page publique puis l'y ramène, la couleur se choisit et se réinitialise. Tout est restauré.
- **`calendar-events.spec.js`** — le calendrier : la visibilité du calendrier de section s'ouvre au public depuis la page de config (enregistrement au changement), le chef crée, modifie puis supprime un évènement via la modale — un seul formulaire, trois verbes, bascule d'identité par champ caché —, les grilles publique et chef **rendent réellement comme des calendriers** (`expectRendersAsAnEventCalendar()` : le pendant, pour `month_grid.html.twig`, de la panne CSS-uniquement que le helper des locations documente), un anonyme ouvre la modale de détails, et le **jeton ICS personnel** du membre est lu sans session, régénéré (l'ancien flux ne sert plus l'agenda — la route répond volontairement 200 vide pour ne pas servir d'oracle d'énumération — le nouveau le porte). Visibilité restaurée, évènement supprimé.
- **`news-lifecycle.spec.js`** — la moitié du cycle de vie où `news-form-payment.spec.js` s'arrête : un article publié est **réouvert dans l'éditeur** (le tour complet `NEWS_EDITOR_DATA` → reconstruction du constructeur → re-sérialisation `fields_json`, le couplage nommé fragile dans `editor.html.twig`), son champ renommé et sa capacité changée sans perdre la réponse déjà enregistrée ; un membre identifié **modifie sa propre réponse** (`canEditResponse` : l'auteur seulement, formulaire ouvert) et la capacité se recalcule ; l'export XLSX des réponses et l'affiche PDF (A4 + QR) sortent comme de vrais fichiers ; l'article est supprimé par le DELETE en fetch de sa modale, et son URL publique répond 404.
- **`finance-receipts.spec.js`** — le trésorier au travail : un compte créé, un **relevé BNP importé par le vrai parseur** (la colonne IBAN doit correspondre au compte, le premier import exige son solde), un reçu photographié dont le gestionnaire de soumission **réencode l'image en JPEG sur un canvas avant le POST**, la recherche en direct qui re-dessine les cartes côté client (le second des deux moteurs de rendu de la grille — Twig peint la première page), et l'association reçu ↔ mouvement dans la modale nourrie par la recherche de mouvements.
- **`mass-mail.spec.js`** — un envoi groupé du brouillon aux boîtes : le dialogue de composition entièrement construit par le script depuis le blob `MM_DATA`, le brouillon enregistré, une pièce jointe (multipart, PDF/images seulement), le passage en mode test et le **message de test réellement reçu**, puis « Lancer l'envoi » (derrière son `confirm()`) et la **livraison réelle** aux membres de la liste par la tâche `send_batch` — pièce jointe comprise — lue dans la boîte de l'exécution, la page de suivi nommant chaque destinataire et son état.
- **`gallery-media.spec.js`** — les médias d'un album : deux photos par le chemin XHR simple et une de **25 Mo par le chemin morcelé** (`chunked-upload.js` — le contrat tranche/index/réassemblage n'existe qu'entre ce script et son consommateur PHP, et le restaurateur de Maintenance est son second consommateur), la réorganisation par glisser-déposer HTML5 persistée au `dragend`, la couverture, une suppression, puis côté membre la grille, la **lightbox** (image décodée, navigation, Échap), et l'album en ZIP. Les vignettes n'existant qu'une fois la tâche `ProcessPhoto` passée, leur apparition est aussi la preuve du pipeline de traitement. L'album est supprimé à la fin.
- **`groups-mentions.spec.js`** — la chaîne que les deux autres scénarios Groupes laissent ouverte : l'autocomplétion de **mention** nourrie au clavier par `/groups/{id}/mention-search`, la notification « Vous êtes cité » dans le centre de notifications de l'autre personne, l'**épinglage** dont le formulaire est intercepté par le dialogue de durée (la réponse part dans le champ caché), le « **Vu par 1 membre** » qui nomme le lecteur dans son dialogue (réservé à l'auteur), et la **galerie du groupe** servant la photo d'un message à travers la vérification d'accès déléguée du module Galerie.
- **`banner-visibility.spec.js`** — une bannière d'accueil : le flux d'ajout en trois fetch (l'enregistrement est créé d'abord pour obtenir un id, rempli dans le dialogue de texte riche partagé, et **supprimé à nouveau si le dialogue est abandonné** — la compensation sur `hidden.bs.modal` qu'aucune autre couche n'exécute), puis son `role_min` : réservée aux chefs, l'anonyme ne la voit jamais sur `/` ; rendue publique, il la voit ; supprimée, plus personne.
- **`cookie-consent-accept.spec.js`** (deux scénarios) — les deux autres réponses à la bannière, en miroir du refus déjà couvert : « Tout accepter » est mémorisé et laisse le cookie fonctionnel `last_login_method` s'écrire à la connexion ; la page `/cookies` accorde une catégorie (l'interrupteur re-rend coché depuis le consentement stocké), et son retrait **expire le cookie couvert hors du navigateur** dans la même réponse (`LastLoginMethodCookie::forget()`) — jamais seulement « on cesse de le lire ».
- **`oncall-grid.spec.js`** — la grille de garde SOS : chaque clic fait tourner la cellule sur ses trois états et **re-poste le mois entier** (aucun bouton, aucune requête par cellule), le rechargement prouve la persistance, et le rendu téléphone de la même cellule (même classe, deux mises en page) a bougé avec. `/config/sos` n'est pas exercé : chaque étape de son assistant appelle la vraie API OVH.
- **`camps-place-and-review.spec.js`** — la chaîne complète du module Camps, qu'aucun test unitaire ne traverse de bout en bout : un même formulaire crée le **lieu** et le **séjour**, le détecteur de doublons a son mot à dire avant que l'un ou l'autre soit écrit, la page du séjour se construit sur quatre dépôts et une frise `Core\Audit`, et l'avis n'est proposé qu'une fois le camp terminé — puis se retrouve à la fois sur le séjour et sur le lieu, ce qui est la raison d'être du module. Un seul scénario plutôt qu'un par écran : le détecteur, les fusions, l'écran courrier et la frontière RBAC de chaque route sont couverts là où ils peuvent l'être exhaustivement (`DuplicatePlaceDetectorTest`, `MergeServiceTest`, `CampsMailControllerTest`, `CampsRbacTest`).
- **`mass-mail-merge.spec.js`** — le publipostage depuis un fichier Excel (ARCHITECTURE.md §8.61), le chemin le plus construit côté navigateur de tout le dialogue d'envoi : un `.xlsx` invalide est refusé avec son rapport ligne à ligne (tout ou rien), un fichier valide voit une ligne résolue par le « Tiers » du Desk et l'autre par une adresse libre, les variables `{{…}}` sont insérées par les deux chemins de la barre d'outils, l'aperçu montre les valeurs **propres à chaque ligne**, et l'envoi réel — par le vrai point d'entrée du planificateur — dépose deux messages *différents* dans deux boîtes, pied de page de désinscription compris pour le destinataire externe. Le rayon d'action d'une régression y est pire que celui de la liste classique : chaque destinataire reçoit un email **faux**, le prénom ou le montant de quelqu'un d'autre.
- **`toast-never-blocks.spec.js`** — un toast annonce, et n'intercepte jamais un clic destiné à la page derrière lui. Le coin bas-droite où `public/assets/js/toast.js` les empile est aussi celui des actions principales et des pieds de dialogue : tant qu'un toast était affiché (4 s pour un succès, 8 s pour une erreur), ce coin était mort au clic. Le scénario mesure ce qu'un clic **atteint réellement** (`elementFromPoint` au centre du toast) plutôt que d'affirmer une déclaration CSS, et vérifie que le bouton de fermeture du toast, lui, reste cliquable. Trouvé par la CI sur `mass-mail-merge.spec.js` ; il passait en local, où une machine plus rapide laissait le toast expirer avant le clic suivant.
- **`breadcrumb-separator.spec.js`** — le séparateur « / » du fil d'Ariane reste sur la ligne de base du texte, sur un appareil **tactile**. Bootstrap le rend comme un `::before` flottant, placé en haut de sa miette : invisible tant que toutes les miettes sont du texte de même hauteur, faux dès qu'une miette est un vrai `<button>` (une entrée `parents` qui correspond à un menu), car `app.css` lui donne 44 px sous `(pointer: coarse)` quand ses voisines en font 24. Le contexte émule donc un téléphone (`hasTouch`, ce qui fait correspondre `(pointer: coarse)`) : une largeur de fenêtre seule ne testerait rien. Signalé depuis un vrai téléphone — un navigateur piloté à la souris correspond à `(pointer: fine)` et ne le montre jamais.

Les scénarios qui agissent en tant que membre (`groups-discussion`, `groups-management`, `groups-mentions`, …) ont besoin d'une identité : un groupe de discussion s'écrit et se lit en tant que *membre*, jamais en tant que compte, et publier exige en plus un prénom et un nom sur le compte (`Modules\Groups\Service\GroupAccessService`). `scripts/e2e-support.php` provisionne donc au super-administrateur une ligne `members`/`member_years` portant la même adresse que son compte, pour l'année scoute en cours, et renseigne ses noms (`e2e_seed_member_for_admin()`) — aucune de ces deux exigences n'est contournée, elles sont satisfaites. Il provisionne aussi un **second membre ordinaire** avec son propre compte (`e2e_seed_ordinary_member()`, mot de passe généré par exécution comme celui de l'administrateur) et une **section** à laquelle les deux appartiennent (`e2e_seed_section_with_both_members()`), ce qui met les deux dans le même groupe de section sans qu'aucun scénario ait à en inviter un. Cette même fonction leur donne une ligne `member_functions` pour une fonction dont le `role` est `identified` — le niveau auquel `Core\Security\RoleResolver` les résout déjà, donc aucun des deux n'y gagne quoi que ce soit, et « un membre ordinaire voit ceci » ne devient jamais silencieusement « un animateur voit ceci ». Elle existe parce qu'un vrai import Desk en écrit toujours une, et parce que `Core\Member\SectionService::getSectionAnimes()`/`getSectionStaff()` lisent la section par les fonctions et non par les périodes : c'est ce que `Modules\Groups\Controller\GroupMemberController` propose à un modérateur comme personnes invitables, et sans elle cette liste est vide. L'année scoute en cours étant celle du fixture, ces scénarios doivent s'exécuter avant `scout-year-transition.spec.js`, qui fait avancer l'année publique : Playwright exécute les fichiers dans l'ordre alphabétique, et `groups-` précède `scout-year-`. C'est le même ordre qui garantit que `optional-module-dependencies.spec.js` a bien remis tous les modules en service avant que `rental-*` et `scout-year-transition` n'en aient besoin. Deux compléments de fixture existent pour la même raison « satisfaire, jamais contourner » : `e2e_seed_unit_chief_function_for_admin()` donne au membre du super-administrateur une fonction **principale** « Chef d'unité » dans la vraie section Staff d'U (créée par `Core\Member\UnitStaffSectionService`, le même code qu'un import Desk) — plusieurs comportements ne s'ouvrent qu'à une *adhésion* de chef d'unité et jamais au drapeau superadmin (la modération des rétrospectives, `MemberService::isUnitChief()`) — et `e2e_seed_mobile_for_admin()` lui renseigne un numéro de mobile fictif, sans lequel `Modules\SosStaff` n'offre aucune colonne dans sa grille de garde. Les scénarios qui mutent un fixture partagé le restaurent avant de finir : `password-reset` et `account-settings` rendent au membre son mot de passe provisionné, `config-desk` son rôle et sa section, `registration-*` laissent la demande dans un état final, `calendar-events` referme la visibilité du calendrier.

**Le courrier de l'exécution.** Plusieurs des parcours les plus importants du site se terminent *dans une boîte mail* et n'ont aucune moitié navigateur : un lien magique, une réinitialisation de mot de passe, la confirmation d'une réponse à un formulaire. L'instance jetable tourne donc en mode courrier **local** et `scripts/e2e.sh` fait pointer le `sendmail_path` de PHP sur `scripts/e2e-maildrop.php`, qui écrit chaque message entier (en-têtes, parties MIME, signature DKIM comprise) dans le répertoire nommé par `E2E_MAILDROP` — à l'intérieur du répertoire temporaire de l'exécution, donc supprimé avec elle. `tests/e2e/support/maildrop.js` le relit.

Ce n'est **pas** un faux expéditeur : ni `Core\Mail\MailService`, ni PHPMailer, ni la signature DKIM, ni les gabarits d'email ne sont remplacés ou contournés — seul le dernier saut du transport est un fichier au lieu d'une socket, exactement comme un `sendmail` local qui écrirait dans un Maildir. Un scénario vérifie donc le message que l'application a réellement produit. Avant cela, l'instance était configurée pour un SMTP sur `localhost:25` que rien n'écoutait : chaque envoi levait une `MailException`, et aucun scénario ne pouvait aller voir de l'autre côté.

**Les templates compilés sont supprimés à chaque provisionnement**, et ce n'est pas du ménage. L'instance tourne avec `debug` à `false` (comme une vraie installation), donc Twig met en cache les templates compilés et `auto_reload` est désactivé ; `Core\View\TwigFactory` indexe ce cache sur la seule `VERSION` — un choix délibéré et correct en production, où une installation se met à jour par version — et l'ancre sur le **dépôt**, pas sur l'instance. Sans cette suppression, un `.twig` modifié depuis la dernière exécution est compilé une fois et plus jamais, et les tests navigateur valident donc un template qui n'est plus dans le dépôt. C'est arrivé. `tests/bootstrap.php` fait la même chose pour PHPUnit, pour la même raison, et `Tests\Bootstrap\TwigCacheFreshnessTest` vérifie que ça continue.

Les assertions utilisent les rôles ARIA et les textes visibles (`getByRole`, `getByLabel`) partout où ils identifient l'élément — et échouent sur toute réponse 5xx ou erreur JavaScript non capturée. Les rares exceptions sont documentées dans le fichier concerné : `groups-discussion.spec.js` s'appuie sur les identifiants et classes auxquels `groups.js` se lie lui-même (donc un contrat, pas une structure incidente) pour les trois éléments qu'aucun rôle ne distingue — le conteneur du fil, le corps *rendu* d'un message (le formulaire d'édition sous la carte contient le même texte dans un `textarea`) et les blocs que les fragments dynamiques remplacent en entier. `groups-management.spec.js` fait de même pour la boîte de recherche d'invitation et pour les lignes de la liste des membres (du balisage `list-group` Bootstrap, donc des `div` sans rôle `listitem`). `news-form-payment.spec.js` et `optional-module-dependencies.spec.js` ne gardent du **constructeur de champs** que le `data-key` que `news-form-builder.js` donne à chaque champ, pour désigner *quel* panneau ils manipulent ; tout ce qui s'y trouve est atteint par son nom accessible. Ces contrôles étaient bâtis en JavaScript avec des `label` sans `for` — ils ne nommaient donc rien, ni pour un lecteur d'écran ni pour un test — et sont désormais correctement associés (`fieldControlId()`/`addLabelledFieldEditRow()`), le champ de texte riche compris (`role="textbox"` + `aria-labelledby`, puisqu'un `label for` ne peut pas viser un `contenteditable`). Les deux scénarios sont du même coup la non-régression de ce nommage, dans un vrai arbre d'accessibilité plutôt que dans un DOM simulé.

**Base de données.** Le harnais réutilise le serveur MySQL déjà joignable (`E2E_DB_HOST`/`E2E_DB_PORT`/`E2E_DB_USER`/`E2E_DB_PASSWORD`, avec repli sur les variables `TEST_DB_*` déjà utilisées par les tests PHPUnit du groupe `database`, puis sur `127.0.0.1:3306` / `root` / mot de passe vide). S'il n'y en a aucun et que Docker est disponible, il démarre un conteneur `mysql:8.0` jetable et le supprime à la fin. Sinon il échoue avec un message explicite — jamais silencieusement.

**Artefacts.** Trace, capture d'écran et vidéo ne sont produites qu'en cas d'échec, sous `tests/e2e/test-results/`, avec un rapport HTML dans `tests/e2e/playwright-report/`. Les deux sont ignorés par git.

**Où ça tourne.** Le navigateur est *headless* : aucune fenêtre, aucune interaction souris/clavier, aucun service à démarrer à la main, aucun chemin absolu propre à une machine. macOS et Linux fonctionnent à l'identique. Depuis Claude Code, ce qui compte est l'hôte d'exécution, pas l'appareil client : une session lancée depuis un iPhone s'exécute dans un environnement de développement distant, et c'est cet hôte (Linux) qui doit pouvoir installer Chromium et joindre un MySQL — iOS lui-même n'exécute jamais Playwright. Si l'hôte distant ne peut pas héberger de binaire navigateur, `npm run e2e` n'y est pas exécutable ; la même commande reste la référence en local et dans GitHub Actions. Cas particulier : un environnement où le téléchargement Playwright est indisponible mais où un Chromium compatible existe déjà sur le disque (p. ex. un conteneur Claude Code distant qui en embarque un sous `/opt/pw-browsers`) peut le désigner via `E2E_CHROMIUM_EXECUTABLE=/chemin/vers/chromium npm run e2e` — non définie (le cas normal), Playwright résout son navigateur géré exactement comme avant.

**Couverture de code.** `E2E_COVERAGE=1 npm run e2e` produit en plus `coverage-e2e.xml` (Clover), fusionné par SonarQube Cloud avec la couverture PHPUnit (`sonar.php.coverage.reportPaths` liste les deux rapports : une ligne couverte par l'un *ou* l'autre compte comme couverte). La collecte se fait avec l'extension **pcov** dans le processus `php -S` lui-même, via `auto_prepend_file` (`scripts/e2e-coverage-prepend.php`) : chaque requête écrit un fragment, et `scripts/e2e-support.php merge-coverage` les fusionne en fin de run. Sans pcov (ni xdebug), la commande échoue explicitement au lieu de produire un rapport vide. C'est la seule couverture qui existe pour `public/index.php` — la racine de composition, que PHPUnit n'exécute jamais et que le navigateur traverse à chaque requête. Le harnais lui-même (`scripts/e2e-support.php`, `scripts/e2e-coverage-prepend.php`) est exclu du calcul de couverture (`sonar.coverage.exclusions`) : c'est de l'infrastructure de test, que rien ne peut couvrir puisqu'elle exécute la suite au lieu d'être exécutée par elle. La collecte n'a **aucune** influence sur le verdict des tests : une fusion ratée est signalée, elle ne fait jamais échouer un run vert. Elle est désactivée par défaut en local (le run est plus rapide) et activée dans CI.

**Playwright et la règle « pas d'outil de build frontend ».** Playwright est de l'outillage de test, au même titre que Vitest : il n'introduit ni bundler, ni compilateur Sass, ni transpileur, ni la moindre étape de build pour `public/assets/js/`, qui reste du JavaScript navigateur simple chargé par une balise `<script src="...">`. Ni Node ni Playwright ne sont nécessaires pour exécuter ou déployer ScoutMagic, et rien de tout cela n'entre dans l'artefact de release. Voir `AGENTS.md` § CSS / frontend.

### Analyse de sécurité dynamique (DAST, OWASP ZAP)

Complément **dynamique** à l'analyse statique : la suite Playwright est rejouée au travers d'un proxy [OWASP ZAP](https://www.zaproxy.org/), qui observe chaque requête et chaque réponse réelles de l'application. Une seule commande, une instance jetable, une base de données dédiée, un démontage complet :

```bash
docker pull ghcr.io/zaproxy/zaproxy:stable   # une fois : l'image ZAP (~1,2 Go)
./scripts/dast.sh --profile=passive
```

**Pourquoi la suite de tests plutôt qu'un *spider*.** L'araignée de ZAP ne sait pas suivre un lien magique reçu par email, ni confirmer une adresse, ni enregistrer une clé numérique. La suite Playwright, elle, traverse déjà ces parcours — elle lit la boîte aux lettres de l'exécution — et le fait en étant connectée sous plusieurs identités. La rejouer à travers un proxy donne donc l'image la plus fidèle qui existe de la surface réellement exposée par ce site.

**Profils** (`--profile=`) :

| Profil | Contenu | Statut |
|---|---|---|
| `passive` | Règles passives uniquement, sur le trafic de la suite Playwright | **disponible** |
| `standard` | `passive` + matrice d'autorisation sur les cinq rôles | à venir |
| `deep` | `standard` + sous-ensemble ciblé de règles actives | à venir |
| `audit` | Analyse active complète, sans budget de temps, jamais un gate | à venir |

**Ce que le script fait, dans l'ordre** : il vérifie ses prérequis (php, npm, `node_modules`, Docker, l'image ZAP, les extensions PHP `openssl` et `pcntl`) et **échoue en indiquant la commande exacte à exécuter — il n'installe jamais rien** ; il génère un certificat auto-signé valable pour cette seule exécution ; il provisionne l'instance jetable par `scripts/e2e-support.php provision`, **exactement le même provisionnement que `npm run e2e`** (jamais une seconde copie) ; il démarre `php -S` puis `scripts/dast-tls-proxy.php` devant lui pour terminer TLS ; il démarre ZAP et lance le plan `tests/dast/zap-passive.yaml` ; il rejoue Playwright au travers de ZAP ; il produit un rapport HTML et un rapport SARIF ; il vérifie que la carte du site est bien remplie ; il rend un code de sortie non nul dès qu'une alerte de niveau **Medium ou supérieur** subsiste.

**Pourquoi du HTTPS, et pourquoi un terminateur TLS maison.** Le drapeau `Secure` du cookie de session et l'en-tête `Strict-Transport-Security` sont invisibles en clair : sans TLS, la moitié de ce que le scan doit vérifier n'est pas observable. Le serveur intégré de PHP ne parle pas TLS, et le harnais s'interdit nginx comme php-fpm — donc `scripts/dast-tls-proxy.php`, un script PHP comme le sont déjà la boîte aux lettres et le collecteur de couverture. Il pose `X-Forwarded-Proto: https` (**en retirant d'abord toute copie envoyée par le client**), que l'instance jetable croit parce que son `config/app.php` active `trust_forwarded_proto` — l'option décrite en `SECURITY.md` § 9, désactivée partout ailleurs. Avant de lancer quoi que ce soit, le script vérifie que l'instance émet réellement HSTS : sans cela le scan passerait son temps à redécouvrir un défaut du harnais et à l'imputer à l'application.

**Pourquoi il ne peut pas entrer en collision avec `npm run e2e`.** `scripts/release.sh` exécute ses gates en sous-shells parallèles, et `scripts/e2e.sh` **supprime** sa base de données au démontage. Le scan utilise donc une base à lui (`scoutmagic_dast`) et ses propres ports. `npm run e2e` est strictement inchangé : le proxy, l'HTTPS et les délais rallongés sont tous derrière des variables d'environnement que seul `scripts/dast.sh` définit, sur le modèle de `E2E_CHROMIUM_EXECUTABLE` qui existait déjà.

**Le piège qui rendrait le scan inutile sans qu'on le voie.** Chromium contourne tout proxy pour les adresses de bouclage. Sans `--proxy-bypass-list=<-loopback>`, ZAP ne voit rien, tous les tests passent, et le rapport est vide — un échec parfaitement silencieux. C'est pourquoi `scripts/dast-support.php assert-sitemap` vérifie, avant de rendre son verdict, que la carte du site contient bien les pages authentifiées listées dans `tests/dast/expected-authenticated-paths.txt`, et fait échouer l'exécution sinon.

**Un scénario instable, connu.** L'étape de réorganisation par glisser-déposer de `tests/e2e/specs/gallery-media.spec.js` échoue par intermittence sous ce harnais — environ une exécution sur trois — le glisser synthétique ne s'enregistrant pas toujours. Ce n'est ni l'application ni la CSP : le même scénario passe systématiquement sous `npm run e2e`, et tout ce que le glisser touche est du `classList` et de l'affectation de propriété CSSOM, que ni le proxy ni la politique ne peuvent affecter. Ce qui change ici, c'est la latence et le moment où la grille se stabilise. À résoudre avant que ce profil ne puisse bloquer une release : un gate qui échoue une fois sur trois sans rien trouver apprend à tout le monde à le relancer — et c'est ainsi qu'une vraie alerte finit relancée elle aussi.

**Faux positifs.** Ils sont neutralisés par des *alert filters* dans le plan ZAP lui-même (`tests/dast/`), chacun accompagné de la raison écrite pour laquelle il en est un — jamais par un fichier de référence d'alertes acceptées. Aujourd'hui il y en a exactement un : l'absence de jeton anti-CSRF sur `POST /api/webhook/github`, l'exception documentée en `SECURITY.md` § 4, authentifiée par une signature HMAC que ZAP ne peut pas voir.

## Intégration continue

Chaque push sur `main` et chaque Pull Request déclenchent `.github/workflows/ci.yml` :

- **`test`** : vérification de syntaxe PHP, `vendor/bin/phpstan analyse`, puis `vendor/bin/phpunit` — **la suite complète, groupe `database` compris** — avec couverture PCOV et un service MySQL. Génère `coverage.xml` (Clover) et `phpunit-report.xml` (JUnit), publiés comme artefacts pour le job `sonarqube`.
- **`javascript-tests`** : `npm ci`, puis analyse statique JavaScript (`npm run typecheck` — voir § Analyse statique JavaScript), puis tests unitaires JavaScript (`npm run test:coverage` — Vitest + jsdom, `tests/js/`), isolés (sans serveur PHP ni base de données) — génère `coverage/js/lcov.info`, publié comme artefact pour le job `sonarqube`. Un échec de l'une ou l'autre étape fait échouer ce check GitHub indépendamment du job `test`.
- **`e2e-tests`** (« End-to-end (browser) ») : les scénarios de bout en bout (`npm run e2e` — la commande canonique, identique en local et dans `scripts/release.sh`), avec un service MySQL et Chromium installé via `npm run e2e:install`. Sans serveur graphique, borné à 20 minutes. Exécuté avec `E2E_COVERAGE=1` et pcov : génère `coverage-e2e.xml` (Clover), publié comme artefact pour le job `sonarqube`. Un échec fait échouer ce check GitHub ; les diagnostics Playwright (trace, capture, vidéo, rapport HTML) sont publiés comme artefact **uniquement en cas d'échec**.
- **`security`** : `composer audit`.
- **`sonarqube`** : analyse [SonarQube Cloud](https://sonarcloud.io/project/overview?id=xdubois-57_scoutmagic) (voir `sonar-project.properties`), à partir de la couverture/du rapport PHP produits par le job `test`, de la couverture PHP de bout en bout produite par le job `e2e-tests` (les deux rapports Clover sont fusionnés par SonarQube) et de la couverture JavaScript (LCOV) produite par le job `javascript-tests` — ni PHPUnit, ni Vitest, ni Playwright ne sont relancés une seconde fois. Le Quality Gate SonarQube fait échouer ce check GitHub s'il n'est pas OK (`-Dsonar.qualitygate.wait=true`).
- **CodeQL** : analyse de code activée au niveau du dépôt (GitHub Advanced Security), indépendante de ce workflow.

SonarQube Cloud est complémentaire à PHPStan, PHPUnit, l'analyse statique JavaScript (`npm run typecheck`), Vitest, `composer audit` et CodeQL — il ne les remplace pas. Sur une Pull Request, SonarQube Cloud décore automatiquement la PR (résumé du Quality Gate et annotations sur les lignes concernées) via son intégration GitHub officielle, à condition que le secret `SONAR_TOKEN` soit configuré (voir « Configuration manuelle requise » ci-dessous).

### Configuration manuelle requise

Le job `sonarqube` nécessite un secret de dépôt **`SONAR_TOKEN`** (Settings > Secrets and variables > Actions), généré depuis SonarQube Cloud. Sans ce secret, le job échoue pour toute PR/push interne au dépôt (les PR venant d'un fork sont ignorées, GitHub n'exposant pas les secrets aux forks). Voir `AGENTS.md` § Releases pour le rôle de ce même token dans le Release Gate.

## Déploiement

### Créer une release (mainteneurs)

```bash
./scripts/release.sh              # créer une nouvelle release (patch par défaut)
./scripts/release.sh --minor      # incrément de version mineure
./scripts/release.sh --major      # incrément de version majeure
```

Publie une release GitHub avec l'artefact d'installation et `bootstrap.php` en tant qu'assets. Nécessite le CLI GitHub (`gh`).

Avant de créer un commit, un tag ou une release, le script exécute six verrous, dans cet ordre — chacun bloque la release en cas d'échec :

1. **Déploiement** : `www.scoutmagic.be` doit déjà avoir installé la release précédente (comparé via `GET /api/version`, exposé publiquement par `Core\Http\Controller\VersionController`) et répondre normalement (code 200, pas d'erreur visible sur la page d'accueil).
2. **Sécurité** : aucun signalement CodeQL ni alerte Dependabot ouvert dans le dépôt (`gh api repos/{owner}/{repo}/code-scanning/alerts` et `.../dependabot/alerts`, filtrés sur `state == "open"`).
3. **Tests** : `vendor/bin/phpstan analyse`, `vendor/bin/phpunit`, l'analyse statique JavaScript (`npm run typecheck` — voir § Analyse statique JavaScript) et les tests unitaires JavaScript (`npm run test:coverage`) doivent tous passer. `--skip-tests-gate` contourne les quatre à la fois — voir `AGENTS.md` § Releases.
4. **Tests de bout en bout** : `npm run e2e` — la page d'accueil publique doit démarrer via le vrai `public/index.php` et s'afficher dans un Chromium *headless* (voir § Tests de bout en bout). C'est le seul verrou qui prouve que la racine de composition démarre réellement. Verrou distinct de « Tests » à dessein : c'est le seul à exiger un serveur MySQL et un binaire navigateur, et un mainteneur qui n'a ni l'un ni l'autre ne doit pas avoir à renoncer aussi à PHPStan/PHPUnit/Vitest pour publier. `--skip-e2e-gate` le contourne — voir `AGENTS.md` § Releases.
5. **Fraîcheur des dépendances** : aucune dépendance Composer directe (`composer outdated --direct`) ni aucune bibliothèque front-end vendorisée (`public/assets/vendor/` — Bootstrap, Bootstrap Icons, Chart.js) ne doit être en retard sur sa dernière version publiée.
6. **SonarQube Cloud** (`scripts/check-sonar-release.sh`) : aucun signalement de sécurité actif (issue d'impact `SECURITY` non résolue, ou Security Hotspot non trié), aucun signalement de sévérité `HIGH` ou supérieure (`BLOCKER`) actif, et le Quality Gate du projet doit être `OK` — pour l'analyse confirmée correspondre exactement au commit en cours de release. **Ce verrou est fail-closed** : un `SONAR_TOKEN` absent, SonarQube Cloud injoignable, une authentification invalide, une réponse invalide, ou l'impossibilité de confirmer qu'une analyse existe pour le commit exact bloquent la release.

   Ce verrou lit `SONAR_TOKEN` depuis l'environnement, ou à défaut depuis `.sonar-token` à la racine du dépôt (une ligne, jamais commité — voir `.gitignore`). S'il est absent des deux et qu'un terminal est attaché, le script le demande de manière interactive (saisie masquée) et propose de l'enregistrer dans `.sonar-token` — mais seulement après avoir vérifié via `git check-ignore` que ce fichier est bien ignoré par git ; sinon il refuse d'écrire le token et bloque la release. Sans terminal (CI, exécution automatisée), l'absence de token bloque directement, comme avant.

Corrigez le problème signalé (ou justifiez son rejet) avant de publier — voir `AGENTS.md` § Releases. Chaque verrou peut être contourné individuellement en cas d'urgence (`--skip-deployment-check`, `--skip-security-gate`, `--skip-tests-gate`, `--skip-e2e-gate`, `--skip-dependency-check`, `--skip-sonar-gate` ; un avertissement est affiché) — voir l'en-tête de `scripts/release.sh` pour le détail de chaque option. Ces options sont réservées à une décision explicite et informée de l'utilisateur, jamais à contourner une découverte réelle.

### Installation sur hébergement mutualisé (administrateurs d'unité)

Aucun SSH, Git ou Composer nécessaire sur le serveur — seulement le FTP, et une seule fois.

1. Téléchargez `bootstrap.php` depuis la [dernière release](https://github.com/xdubois-57/scoutmagic/releases/latest).
2. Envoyez-le par FTP dans le dossier web vide que votre hébergeur sert comme racine du document.
3. Ouvrez-le dans un navigateur (ex. `https://votre-domaine.be/bootstrap.php`). Il télécharge la dernière release, l'installe, et exécute une vérification de sécurité complète avant de vous montrer quoi que ce soit d'autre — l'écran de confirmation explique lequel des deux types d'installation il a choisi pour votre hébergeur et pourquoi.
4. Cliquez sur **Installer**. Il rapporte la progression étape par étape, puis un tableau succès/échec pour chaque vérification de sécurité effectuée (y compris des vérifications que votre propre navigateur a effectuées en récupérant des URLs directement). Tout échec annule proprement l'installation et explique quoi corriger — rien n'est laissé à moitié installé.
5. Une fois toutes les vérifications passées, il écrit un fichier `token.php` à côté de lui-même (ou vous indique de le créer manuellement par FTP s'il n'a pas pu), se supprime lui-même, et vous redirige vers l'assistant de configuration.
6. L'assistant de configuration demande la valeur du fichier `token.php` avant de montrer quoi que ce soit d'autre — copiez-la depuis le fichier par FTP si vous ne l'avez pas notée. Il est supprimé automatiquement une fois l'assistant terminé.
7. Complétez l'assistant : identifiants de base de données, paramètres de l'unité, configuration email, et votre compte administrateur.

**Activer les mises à jour automatiques** : une fois installé, allez dans *Configuration > Maintenance* et générez un secret de webhook GitHub. Dans les *Settings > Webhooks* de votre dépôt GitHub, ajoutez un webhook avec :
- **Payload URL** : `https://votre-domaine.be/api/webhook/github`
- **Content type** : `application/json`
- **Secret** : la valeur générée sur la page Maintenance
- **Events** : sélectionnez uniquement *Releases*

Sans cela, le site n'apprend jamais qu'une nouvelle release existe — voir ARCHITECTURE.md §8.17 pour le fonctionnement de l'installation des mises à jour une fois notifié.

## Architecture

Voir [ARCHITECTURE.md](ARCHITECTURE.md) pour la référence architecturale complète.

## Sécurité

Voir [SECURITY.md](SECURITY.md) pour les exigences de sécurité.

## Contribuer

Voir [CONTRIBUTING.md](CONTRIBUTING.md) pour les recommandations de contribution.

## Développement de modules

Voir [docs/module-development.md](docs/module-development.md) pour savoir comment créer un module.

## Guides

- [docs/rental-guide.md](docs/rental-guide.md) — louer les biens de l'unité, pour le Staff d'U et les gestionnaires
- [docs/inbound-mail-setup.md](docs/inbound-mail-setup.md) — connecter une boîte mail en lecture seule, pour le superadministrateur

## Licence

[AGPL-3.0](LICENSE)

Ce projet est mis à disposition des unités scoutes et de la communauté, avec l'attente
que tout usage reste open source.
