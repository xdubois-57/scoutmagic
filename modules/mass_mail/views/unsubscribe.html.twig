{% extends "base.html.twig" %}

{% block title %}Désinscription des emails groupés — {{ site_name }}{% endblock %}

{% block content %}
<div class="page-narrow">
    {% include 'partials/page_header.html.twig' with { title: 'Désinscription des emails groupés' } only %}

    <div class="card">
        <div class="card-body text-center py-4">
            {% if state == 'confirm' %}
                <p class="mb-3">Voulez-vous vraiment vous désinscrire des emails groupés envoyés à cette adresse ? Vous pourrez vous réinscrire à tout moment depuis votre espace animés.</p>
                <form method="post" action="/mass-mail/unsubscribe/{{ recipient_id }}">
                    <input type="hidden" name="token" value="{{ token }}">
                    <button type="submit" class="btn btn-outline-danger">Se désinscrire</button>
                </form>
            {% elseif state == 'done' %}
                <p class="text-success mb-0"><i class="bi bi-check-circle" aria-hidden="true"></i> Vous avez bien été désinscrit(e) des emails groupés pour cette adresse.</p>
            {% else %}
                <p class="text-danger mb-0">Ce lien de désinscription n'est plus valide.</p>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
