{#
  Config sub-page picker — the shared partials/page_picker.html.twig
  (a nav rail underneath), same component as the finance page picker and
  the section/calendar pickers. Used to be a hand-drawn row of pill
  buttons.
#}
{% include 'partials/page_picker.html.twig' with {
    picker_id: 'finance-config-picker',
    pages: [
        {'url': '/config/finance', 'label': 'Vue d’ensemble'},
        {'url': '/config/finance/accounts', 'label': 'Comptes'},
        {'url': '/config/finance/categories', 'label': 'Catégories'},
    ],
    current_path: current_path,
    sheet_title: 'Choisir une page',
} only %}
