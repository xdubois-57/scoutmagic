Nouvelles réponses — {{ article_title }}

{{ count }} nouvelle{{ count > 1 ? 's' : '' }} réponse{{ count > 1 ? 's' : '' }} {{ count > 1 ? 'ont' : 'a' }} été soumise{{ count > 1 ? 's' : '' }} depuis le dernier résumé.

Voir les réponses : {{ responses_url }}
